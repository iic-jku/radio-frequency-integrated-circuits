::: {.content-hidden}
Copyright (C) 2025 Harald Pretl and co-authors (harald.pretl@jku.at)

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
:::

```{python}
#| label: fig-npath-filter
#| echo: false
#| fig-cap: "A 4-phase N-path filter."
import schemdraw as sd
import schemdraw.elements as elm

sd.svgconfig.svg2 = False

with sd.Drawing(canvas='svg') as d:
    d.config(unit=2, fontsize=14)

    elm.Line().right(d.unit/2).idot(open=True).label(r'$Z_\mathrm{in}$', 'left').dot()
    d.push()
    elm.Line().up(d.unit*3/4).dot()
    d.push()
    elm.Line().up(d.unit*1.5)
    elm.Line().right(d.unit/2)
    M1 = elm.AnalogNFet(offset_gate=False).reverse().anchor('source').drop('drain').theta(270)
    elm.Line().right(d.unit/4)
    elm.Capacitor().down().length(d.unit*3/4).label(r'$C$')
    elm.Ground()
    elm.Line().up(d.unit/4).at(M1.gate).label(r'$\text{LO}_{0}$', 'right').dot(open=True)
    d.pop()

    elm.Line().right(d.unit/2)
    M2 = elm.AnalogNFet(offset_gate=False).reverse().anchor('source').drop('drain').theta(270)
    elm.Line().right(d.unit/4)
    elm.Capacitor().down().length(d.unit*3/4).label(r'$C$')
    elm.Ground()
    elm.Line().up(d.unit/4).at(M2.gate).label(r'$\text{LO}_{90}$', 'right').dot(open=True)

    d.pop()

    elm.Line().down(d.unit*3/4).dot()
    d.push()
    elm.Line().down(d.unit*1.5)
    elm.Line().right(d.unit/2)
    M3 = elm.AnalogNFet(offset_gate=False).reverse().anchor('source').drop('drain').theta(270)
    elm.Line().right(d.unit/4)
    elm.Capacitor().down().length(d.unit*3/4).label(r'$C$')
    elm.Ground()
    elm.Line().up(d.unit/4).at(M3.gate).label(r'$\text{LO}_{270}$', 'right').dot(open=True)
    d.pop()

    elm.Line().right(d.unit/2)
    M4 = elm.AnalogNFet(offset_gate=False).reverse().anchor('source').drop('drain').theta(270)
    elm.Line().right(d.unit/4)
    elm.Capacitor().down().length(d.unit*3/4).label(r'$C$')
    elm.Ground()
    elm.Line().up(d.unit/4).at(M4.gate).label(r'$\text{LO}_{180}$', 'right').dot(open=True)
```
